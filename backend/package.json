{
  "name": "destiloc-api",
  "version": "0.1.0",
  "private": true,
  "description": "API backend do protótipo de rastreabilidade de bebidas destiladas (TCC)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc",
    "start": "node dist/main.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "db:schema": "psql $DATABASE_URL -f src/db/schema.sql"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "dotenv": "^16.6.1",
    "fastify": "^5.3.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
