# Copie este arquivo para ".env" e ajuste se necessário. Os valores abaixo já
# são os padrões de um `hardhat node` local.

# RPC usada pela consulta PÚBLICA (sem MetaMask) em cada rede.
VITE_RPC_URL_LOCALHOST=http://127.0.0.1:8545
VITE_RPC_URL_SEPOLIA=https://sepolia.infura.io/v3/SEU_PROJECT_ID

# JWT do Pinata, usado para enviar os metadados ao IPFS.
# Gere em https://app.pinata.cloud/developers/api-keys
# Não versione o valor real — mantenha este arquivo apenas com o placeholder.
VITE_PINATA_JWT=SEU_JWT_AQUI
