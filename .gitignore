node_modules/
artifacts/
cache/
coverage/
typechain-types/
dist/

coverage.json

.env
.env.local
.env.*.local

*.log

# Registro de implantação local: efêmero, muda a cada `hardhat node` novo.
# O registro da Sepolia (implantacao-sepolia.json) permanece versionado,
# como evidência da implantação.
/contratos/implantacao-localhost.json

# ABIs versionadas junto com o código: geradas por `npm run compile` +
# `npm run frontend:abi`. Endereço local é efêmero; Sepolia é permanente.
/frontend/src/contracts/enderecos.localhost.json